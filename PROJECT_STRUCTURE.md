# 项目目录结构

```
clash-for-linux-install/
├── docker/                      # Docker 部署相关文件
│   ├── Dockerfile               # Docker 镜像构建文件
│   ├── docker-compose.yml       # Docker Compose 编排配置
│   ├── docker-entrypoint.sh     # 容器入口脚本
│   ├── docker-start.sh          # 快速启动脚本
│   ├── .dockerignore            # 构建忽略文件
│   ├── .env.example             # 环境变量配置模板
│   ├── DOCKER.md                # Docker 部署详细文档
│   └── README.md                # Docker 使用说明
│
├── scripts/                     # 核心脚本目录
│   ├── preflight.sh             # 安装前准备逻辑
│   ├── cmd/                     # 命令实现
│   │   ├── clashctl.sh          # 核心命令(clashon/clashoff等)
│   │   ├── common.sh            # 通用工具函数
│   │   └── clashctl.fish        # Fish shell 补全
│   └── init/                    # 初始化系统配置
│       ├── systemd.sh           # systemd 服务模板
│       ├── SysVinit.sh          # SysVinit 脚本模板
│       ├── OpenRC.sh            # OpenRC 脚本模板
│       └── runit.sh             # runit 脚本模板
│
├── resources/                   # 资源文件目录
│   ├── mixin.yaml               # Mixin 配置模板
│   ├── config.yaml              # 默认订阅配置模板
│   ├── profiles.yaml            # 订阅元数据存储
│   ├── profiles/                # 订阅配置存储目录
│   ├── zip/                     # 二进制文件存储(内核、yq、subconverter)
│   ├── Country.mmdb             # GeoIP 数据库
│   └── geosite.dat              # GeoSite 规则
│
├── install.sh                   # 主安装脚本
├── uninstall.sh                 # 卸载脚本
│
├── README.md                    # 主文档
├── CLAUDE.md                    # Claude Code 项目指南
├── LICENSE                      # 许可证
│
├── .env                         # 安装配置文件
├── .env.example                 # 配置文件示例
├── .editorconfig                # 编辑器配置
├── .shellcheckrc                # ShellCheck 配置
├── .gitignore                   # Git 忽略文件
│
└── folder-alias.json            # 文件夹别名配置(用户自定义)
    private-folder-alias.json    # 私有文件夹别名配置
```

## 目录说明

### `/docker` - Docker 部署
包含所有 Docker 相关的配置和脚本,用于容器化部署。

### `/scripts` - 核心脚本
项目的核心逻辑实现:
- **preflight.sh**: 安装前的环境检查、依赖下载、架构识别
- **cmd/**: 所有 clashctl 命令的实现
- **init/**: 各种 init 系统的服务配置模板

### `/resources` - 资源文件
包含配置模板、规则数据库等静态资源。

### 根目录文件
- **install.sh/uninstall.sh**: 主安装和卸载入口
- **README.md**: 用户文档
- **CLAUDE.md**: AI 助手的项目指南(必须保留在根目录)
- **.env**: 安装配置文件

## 快速导航

- 📖 [使用文档](README.md)
- 🐳 [Docker 部署](docker/README.md)
- 🤖 [开发指南](CLAUDE.md)
